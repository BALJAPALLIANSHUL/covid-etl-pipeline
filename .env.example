# .env.example
# This file is a template for the .env file.
# Copy this file to .env and fill in your local database credentials.
# The .env file is ignored by git and should NOT be committed.

# Your local PostgreSQL server details
DB_HOST=localhost
DB_PORT=5432
DB_NAME=covid_data
DB_USER=user
DB_PASSWORD=password
